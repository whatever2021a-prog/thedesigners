<!doctype html>
<html lang="en">
 <head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>AI Neon Hub</title>
  <script src="/_sdk/element_sdk.js"></script>
  <link href="https://fonts.googleapis.com/css2?family=Orbitron:wght@400;700;900&amp;family=Rajdhani:wght@300;400;500;600;700&amp;family=Exo+2:wght@300;400;500;600;700&amp;family=Share+Tech+Mono&amp;display=swap" rel="stylesheet">
  <style>
        body {
            box-sizing: border-box;
            margin: 0;
            padding: 0;
            font-family: 'Orbitron', 'Rajdhani', 'Exo 2', monospace;
            background: #000000;
            color: #00ffff;
            overflow-x: hidden;
            height: 100%;
        }

        html {
            height: 100%;
        }

        .neon-glow {
            text-shadow: 0 0 5px #00ffff, 0 0 10px #00ffff, 0 0 15px #00ffff, 0 0 20px #00ffff;
        }

        .neon-border {
            border: 2px solid #00ffff;
            box-shadow: 0 0 10px #00ffff, inset 0 0 10px rgba(0, 255, 255, 0.1);
        }

        .hero-section {
            min-height: 100%;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            text-align: center;
            padding: 2rem;
            background: radial-gradient(circle at center, rgba(0, 255, 255, 0.1) 0%, #000000 70%);
            position: relative;
        }

        .hero-section::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: 
                linear-gradient(45deg, transparent 49%, rgba(0, 255, 255, 0.03) 50%, transparent 51%),
                linear-gradient(-45deg, transparent 49%, rgba(0, 255, 255, 0.03) 50%, transparent 51%);
            background-size: 20px 20px;
            pointer-events: none;
        }

        .main-title {
            font-size: 4rem;
            font-weight: bold;
            margin-bottom: 1rem;
            animation: pulse-glow 2s ease-in-out infinite alternate;
        }

        .subtitle {
            font-size: 1.5rem;
            margin-bottom: 2rem;
            opacity: 0.8;
        }

        .hero-description {
            font-size: 1.1rem;
            max-width: 600px;
            margin-bottom: 3rem;
            line-height: 1.6;
            opacity: 0.9;
        }

        .cta-button {
            background: transparent;
            color: #00ffff;
            border: 2px solid #00ffff;
            padding: 1rem 2rem;
            font-size: 1.1rem;
            font-family: inherit;
            cursor: pointer;
            transition: all 0.3s ease;
            text-transform: uppercase;
            letter-spacing: 2px;
            position: relative;
            overflow: hidden;
        }

        .cta-button:hover {
            background: rgba(0, 255, 255, 0.1);
            box-shadow: 0 0 20px #00ffff;
            transform: translateY(-2px);
        }

        .cta-button:active {
            transform: translateY(0);
        }

        .content-section {
            padding: 4rem 2rem;
            max-width: 1200px;
            margin: 0 auto;
        }

        .ai-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 3rem;
            margin-top: 3rem;
        }

        .ai-card {
            background: rgba(0, 255, 255, 0.05);
            border: 1px solid rgba(0, 255, 255, 0.3);
            padding: 2rem;
            border-radius: 10px;
            transition: all 0.3s ease;
            position: relative;
        }

        .ai-card:hover {
            border-color: #00ffff;
            box-shadow: 0 0 20px rgba(0, 255, 255, 0.3);
            transform: translateY(-5px);
        }

        .ai-card h3 {
            font-size: 1.5rem;
            margin-bottom: 1rem;
            color: #00ffff;
        }

        .ai-card p {
            line-height: 1.6;
            opacity: 0.8;
        }

        .circuit-pattern {
            position: absolute;
            top: 10px;
            right: 10px;
            width: 30px;
            height: 30px;
            opacity: 0.3;
        }

        @keyframes pulse-glow {
            from {
                text-shadow: 0 0 5px #00ffff, 0 0 10px #00ffff, 0 0 15px #00ffff;
            }
            to {
                text-shadow: 0 0 10px #00ffff, 0 0 20px #00ffff, 0 0 30px #00ffff, 0 0 40px #00ffff;
            }
        }

        @keyframes float {
            0%, 100% { transform: translateY(0px); }
            50% { transform: translateY(-10px); }
        }

        .floating {
            animation: float 3s ease-in-out infinite;
        }

        .page {
            display: none;
        }

        .page.active {
            display: block;
        }

        .ai-details-page {
            min-height: 100%;
            background: linear-gradient(135deg, #000000 0%, #001122 50%, #000000 100%);
            padding: 2rem;
        }

        .back-button {
            background: transparent;
            color: #00ffff;
            border: 1px solid #00ffff;
            padding: 0.5rem 1rem;
            font-family: 'Share Tech Mono', monospace;
            cursor: pointer;
            margin-bottom: 2rem;
            transition: all 0.3s ease;
            text-transform: uppercase;
            letter-spacing: 1px;
        }

        .back-button:hover {
            background: rgba(0, 255, 255, 0.1);
            box-shadow: 0 0 10px #00ffff;
        }

        .ai-showcase {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(350px, 1fr));
            gap: 2rem;
            max-width: 1400px;
            margin: 0 auto;
        }

        .ai-feature {
            background: linear-gradient(145deg, rgba(0, 255, 255, 0.1), rgba(0, 255, 255, 0.05));
            border: 1px solid rgba(0, 255, 255, 0.3);
            border-radius: 15px;
            padding: 2rem;
            position: relative;
            overflow: hidden;
            transition: all 0.4s ease;
        }

        .ai-feature:hover {
            transform: translateY(-10px) scale(1.02);
            border-color: #00ffff;
            box-shadow: 0 20px 40px rgba(0, 255, 255, 0.2);
        }

        .ai-feature::before {
            content: '';
            position: absolute;
            top: -50%;
            left: -50%;
            width: 200%;
            height: 200%;
            background: linear-gradient(45deg, transparent, rgba(0, 255, 255, 0.1), transparent);
            transform: rotate(45deg);
            transition: all 0.6s ease;
            opacity: 0;
        }

        .ai-feature:hover::before {
            opacity: 1;
            animation: shimmer 1.5s ease-in-out;
        }

        .feature-icon {
            width: 60px;
            height: 60px;
            margin-bottom: 1rem;
            color: #00ffff;
            filter: drop-shadow(0 0 10px #00ffff);
        }

        .feature-title {
            font-family: 'Rajdhani', sans-serif;
            font-size: 1.8rem;
            font-weight: 600;
            margin-bottom: 1rem;
            color: #00ffff;
            text-transform: uppercase;
            letter-spacing: 2px;
        }

        .feature-description {
            font-family: 'Exo 2', sans-serif;
            line-height: 1.6;
            opacity: 0.9;
            margin-bottom: 1.5rem;
        }

        .feature-stats {
            display: flex;
            justify-content: space-between;
            font-family: 'Share Tech Mono', monospace;
            font-size: 0.9rem;
            opacity: 0.7;
        }

        .page-title {
            font-family: 'Orbitron', sans-serif;
            font-size: 3rem;
            font-weight: 900;
            text-align: center;
            margin-bottom: 3rem;
            background: linear-gradient(45deg, #00ffff, #0099cc, #00ffff);
            background-size: 200% 200%;
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
            animation: gradient-shift 3s ease-in-out infinite;
        }

        @keyframes shimmer {
            0% { transform: translateX(-100%) translateY(-100%) rotate(45deg); }
            100% { transform: translateX(100%) translateY(100%) rotate(45deg); }
        }

        @keyframes gradient-shift {
            0%, 100% { background-position: 0% 50%; }
            50% { background-position: 100% 50%; }
        }

        @media (max-width: 768px) {
            .main-title {
                font-size: 2.5rem;
            }
            
            .hero-section {
                padding: 1rem;
            }
            
            .content-section {
                padding: 2rem 1rem;
            }

            .page-title {
                font-size: 2rem;
            }

            .ai-showcase {
                grid-template-columns: 1fr;
                gap: 1.5rem;
            }

            .ai-details-page {
                padding: 1rem;
            }
        }
    </style>
  <style>@view-transition { navigation: auto; }</style>
  <script src="/_sdk/data_sdk.js" type="text/javascript"></script>
  <script src="https://cdn.tailwindcss.com" type="text/javascript"></script>
 </head>
 <body>
  <main><!-- Home Page -->
   <div id="home-page" class="page active">
    <section class="hero-section">
     <h1 id="main-title" class="main-title neon-glow">Artificial Intelligence</h1>
     <h2 id="subtitle" class="subtitle">The Future is Now</h2>
     <p id="hero-description" class="hero-description">Explore the cutting-edge world of artificial intelligence, where innovation meets possibility. Discover how AI is transforming industries, enhancing human capabilities, and shaping tomorrow's digital landscape.</p><button id="cta-button" class="cta-button neon-border floating">Explore AI</button>
    </section>
    <section class="content-section">
     <div class="ai-grid">
      <div class="ai-card">
       <svg class="circuit-pattern" viewbox="0 0 24 24" fill="currentColor"><path d="M12 2L2 7V10C2 16 6 20.5 12 22C18 20.5 22 16 22 10V7L12 2Z" />
       </svg>
       <h3 id="section-one-title">Machine Learning</h3>
       <p id="section-one-text">Advanced algorithms that learn from data, enabling computers to make predictions, recognize patterns, and improve performance without explicit programming.</p>
      </div>
      <div class="ai-card">
       <svg class="circuit-pattern" viewbox="0 0 24 24" fill="currentColor"><path d="M9 12L11 14L15 10M21 12C21 16.97 16.97 21 12 21C7.03 21 3 16.97 3 12C3 7.03 7.03 3 12 3C16.97 3 21 7.03 21 12Z" />
       </svg>
       <h3 id="section-two-title">Neural Networks</h3>
       <p id="section-two-text">Interconnected processing nodes inspired by the human brain, capable of learning complex patterns and solving sophisticated problems across multiple domains.</p>
      </div>
     </div>
    </section>
   </div><!-- AI Details Page -->
   <div id="ai-details-page" class="page">
    <div class="ai-details-page"><button id="back-button" class="back-button">← Back to Home</button>
     <h1 class="page-title">AI Technologies</h1>
     <div class="ai-showcase">
      <div class="ai-feature">
       <svg class="feature-icon" viewbox="0 0 24 24" fill="currentColor"><path d="M12 2C13.1 2 14 2.9 14 4C14 5.1 13.1 6 12 6C10.9 6 10 5.1 10 4C10 2.9 10.9 2 12 2ZM21 9V7L15 1L13.5 2.5L16.17 5.17L10.5 10.84L6.83 7.17L2 12L3.41 13.41L6.83 10L10.5 13.67L16.17 8L18.83 10.67L17 12.5L23 18.5V16.5L19.5 13L21 9Z" />
       </svg>
       <h3 class="feature-title">Deep Learning</h3>
       <p class="feature-description">Multi-layered neural networks that can automatically learn hierarchical representations from raw data, enabling breakthrough performance in image recognition, natural language processing, and complex decision making.</p>
       <div class="feature-stats"><span>Accuracy: 99.7%</span> <span>Speed: Real-time</span>
       </div>
      </div>
      <div class="ai-feature">
       <svg class="feature-icon" viewbox="0 0 24 24" fill="currentColor"><path d="M12 2L13.09 8.26L22 9L13.09 9.74L12 16L10.91 9.74L2 9L10.91 8.26L12 2Z" />
       </svg>
       <h3 class="feature-title">Computer Vision</h3>
       <p class="feature-description">Advanced image and video analysis capabilities that enable machines to interpret and understand visual information, from facial recognition to autonomous vehicle navigation and medical imaging diagnostics.</p>
       <div class="feature-stats"><span>Objects: 10,000+</span> <span>Processing: 60 FPS</span>
       </div>
      </div>
      <div class="ai-feature">
       <svg class="feature-icon" viewbox="0 0 24 24" fill="currentColor"><path d="M12 14C9.79 14 8 15.79 8 18S9.79 22 12 22 16 20.21 16 18 14.21 14 12 14M12 12C14.21 12 16 10.21 16 8S14.21 4 12 4 8 5.79 8 8 9.79 12 12 12M12 2C15.31 2 18 4.69 18 8C18 11.31 15.31 14 12 14C8.69 14 6 11.31 6 8C6 4.69 8.69 2 12 2Z" />
       </svg>
       <h3 class="feature-title">Natural Language</h3>
       <p class="feature-description">Sophisticated language understanding and generation systems that can comprehend context, sentiment, and intent, enabling human-like conversations, translation, and content creation across multiple languages.</p>
       <div class="feature-stats"><span>Languages: 100+</span> <span>Context: 32K tokens</span>
       </div>
      </div>
      <div class="ai-feature">
       <svg class="feature-icon" viewbox="0 0 24 24" fill="currentColor"><path d="M19 3H5C3.9 3 3 3.9 3 5V19C3 20.1 3.9 21 5 21H19C20.1 21 21 20.1 21 19V5C21 3.9 20.1 3 19 3M19 19H5V5H19V19M17 12C17 14.76 14.76 17 12 17S7 14.76 7 12 9.24 7 12 7 17 9.24 17 12M12 9C10.34 9 9 10.34 9 12S10.34 15 12 15 15 13.66 15 12 13.66 9 12 9Z" />
       </svg>
       <h3 class="feature-title">Robotics AI</h3>
       <p class="feature-description">Intelligent robotic systems that combine perception, planning, and control to interact with the physical world, from industrial automation to service robots and autonomous exploration vehicles.</p>
       <div class="feature-stats"><span>Precision: ±0.1mm</span> <span>Uptime: 99.9%</span>
       </div>
      </div>
      <div class="ai-feature">
       <svg class="feature-icon" viewbox="0 0 24 24" fill="currentColor"><path d="M9.5 3A6.5 6.5 0 0 1 16 9.5C16 11.11 15.41 12.59 14.44 13.73L14.71 14H15.5L20.5 19L19 20.5L14 15.5V14.71L13.73 14.44C12.59 15.41 11.11 16 9.5 16A6.5 6.5 0 0 1 3 9.5A6.5 6.5 0 0 1 9.5 3M9.5 5C7 5 5 7 5 9.5S7 14 9.5 14 14 12 14 9.5 12 5 9.5 5Z" />
       </svg>
       <h3 class="feature-title">Predictive Analytics</h3>
       <p class="feature-description">Advanced forecasting systems that analyze historical patterns and real-time data to predict future trends, enabling proactive decision-making in finance, healthcare, supply chain, and business intelligence.</p>
       <div class="feature-stats"><span>Forecast: 95% accurate</span> <span>Data Points: 1M+</span>
       </div>
      </div>
      <div class="ai-feature">
       <svg class="feature-icon" viewbox="0 0 24 24" fill="currentColor"><path d="M12 2C6.48 2 2 6.48 2 12S6.48 22 12 22 22 17.52 22 12 17.52 2 12 2M12 20C7.59 20 4 16.41 4 12S7.59 4 12 4 20 7.59 20 12 16.41 20 12 20M15.88 8.29L10 14.17L8.12 12.29L6.71 13.71L10 17L17.29 9.71L15.88 8.29Z" />
       </svg>
       <h3 class="feature-title">Autonomous Systems</h3>
       <p class="feature-description">Self-governing AI systems capable of independent decision-making and adaptation in dynamic environments, from self-driving vehicles to smart city infrastructure and automated trading systems.</p>
       <div class="feature-stats"><span>Decisions: 1000/sec</span> <span>Reliability: 99.99%</span>
       </div>
      </div>
     </div>
    </div>
   </div>
  </main>
  <script>
        const defaultConfig = {
            background_color: "#000000",
            neon_color: "#00ffff",
            surface_color: "rgba(0, 255, 255, 0.05)",
            text_color: "#00ffff",
            accent_color: "rgba(0, 255, 255, 0.3)",
            font_family: "Orbitron",
            font_size: 16,
            main_title: "Artificial Intelligence",
            subtitle: "The Future is Now",
            hero_description: "Explore the cutting-edge world of artificial intelligence, where innovation meets possibility. Discover how AI is transforming industries, enhancing human capabilities, and shaping tomorrow's digital landscape.",
            section_one_title: "Machine Learning",
            section_one_text: "Advanced algorithms that learn from data, enabling computers to make predictions, recognize patterns, and improve performance without explicit programming.",
            section_two_title: "Neural Networks",
            section_two_text: "Interconnected processing nodes inspired by the human brain, capable of learning complex patterns and solving sophisticated problems across multiple domains.",
            cta_button_text: "Explore AI"
        };

        async function onConfigChange(config) {
            const baseSize = config.font_size || defaultConfig.font_size;
            const customFont = config.font_family || defaultConfig.font_family;
            const baseFontStack = 'Courier New, monospace';
            
            // Update colors
            document.body.style.background = config.background_color || defaultConfig.background_color;
            document.body.style.color = config.text_color || defaultConfig.text_color;
            
            // Update neon effects
            const neonColor = config.neon_color || defaultConfig.neon_color;
            const neonElements = document.querySelectorAll('.neon-glow, .main-title');
            neonElements.forEach(el => {
                el.style.textShadow = `0 0 5px ${neonColor}, 0 0 10px ${neonColor}, 0 0 15px ${neonColor}, 0 0 20px ${neonColor}`;
            });
            
            const neonBorders = document.querySelectorAll('.neon-border, .cta-button');
            neonBorders.forEach(el => {
                el.style.borderColor = neonColor;
                el.style.color = neonColor;
                el.style.boxShadow = `0 0 10px ${neonColor}, inset 0 0 10px ${config.accent_color || defaultConfig.accent_color}`;
            });
            
            // Update surfaces
            const surfaceColor = config.surface_color || defaultConfig.surface_color;
            const cards = document.querySelectorAll('.ai-card');
            cards.forEach(card => {
                card.style.background = surfaceColor;
            });
            
            // Update fonts
            document.body.style.fontFamily = `${customFont}, ${baseFontStack}`;
            
            // Update font sizes
            document.getElementById('main-title').style.fontSize = `${baseSize * 2.5}px`;
            document.getElementById('subtitle').style.fontSize = `${baseSize * 1.5}px`;
            document.getElementById('hero-description').style.fontSize = `${baseSize * 1.1}px`;
            document.getElementById('cta-button').style.fontSize = `${baseSize * 1.1}px`;
            
            const cardTitles = document.querySelectorAll('.ai-card h3');
            cardTitles.forEach(title => {
                title.style.fontSize = `${baseSize * 1.5}px`;
            });
            
            // Update text content
            document.getElementById('main-title').textContent = config.main_title || defaultConfig.main_title;
            document.getElementById('subtitle').textContent = config.subtitle || defaultConfig.subtitle;
            document.getElementById('hero-description').textContent = config.hero_description || defaultConfig.hero_description;
            document.getElementById('section-one-title').textContent = config.section_one_title || defaultConfig.section_one_title;
            document.getElementById('section-one-text').textContent = config.section_one_text || defaultConfig.section_one_text;
            document.getElementById('section-two-title').textContent = config.section_two_title || defaultConfig.section_two_title;
            document.getElementById('section-two-text').textContent = config.section_two_text || defaultConfig.section_two_text;
            document.getElementById('cta-button').textContent = config.cta_button_text || defaultConfig.cta_button_text;
        }

        function mapToCapabilities(config) {
            return {
                recolorables: [
                    {
                        get: () => config.background_color || defaultConfig.background_color,
                        set: (value) => {
                            config.background_color = value;
                            window.elementSdk.setConfig({ background_color: value });
                        }
                    },
                    {
                        get: () => config.neon_color || defaultConfig.neon_color,
                        set: (value) => {
                            config.neon_color = value;
                            window.elementSdk.setConfig({ neon_color: value });
                        }
                    },
                    {
                        get: () => config.surface_color || defaultConfig.surface_color,
                        set: (value) => {
                            config.surface_color = value;
                            window.elementSdk.setConfig({ surface_color: value });
                        }
                    },
                    {
                        get: () => config.text_color || defaultConfig.text_color,
                        set: (value) => {
                            config.text_color = value;
                            window.elementSdk.setConfig({ text_color: value });
                        }
                    },
                    {
                        get: () => config.accent_color || defaultConfig.accent_color,
                        set: (value) => {
                            config.accent_color = value;
                            window.elementSdk.setConfig({ accent_color: value });
                        }
                    }
                ],
                borderables: [],
                fontEditable: {
                    get: () => config.font_family || defaultConfig.font_family,
                    set: (value) => {
                        config.font_family = value;
                        window.elementSdk.setConfig({ font_family: value });
                    }
                },
                fontSizeable: {
                    get: () => config.font_size || defaultConfig.font_size,
                    set: (value) => {
                        config.font_size = value;
                        window.elementSdk.setConfig({ font_size: value });
                    }
                }
            };
        }

        function mapToEditPanelValues(config) {
            return new Map([
                ["main_title", config.main_title || defaultConfig.main_title],
                ["subtitle", config.subtitle || defaultConfig.subtitle],
                ["hero_description", config.hero_description || defaultConfig.hero_description],
                ["section_one_title", config.section_one_title || defaultConfig.section_one_title],
                ["section_one_text", config.section_one_text || defaultConfig.section_one_text],
                ["section_two_title", config.section_two_title || defaultConfig.section_two_title],
                ["section_two_text", config.section_two_text || defaultConfig.section_two_text],
                ["cta_button_text", config.cta_button_text || defaultConfig.cta_button_text]
            ]);
        }

        // Initialize SDK
        if (window.elementSdk) {
            window.elementSdk.init({
                defaultConfig,
                onConfigChange,
                mapToCapabilities,
                mapToEditPanelValues
            });
        }

        // Page navigation functions
        function showPage(pageId) {
            document.querySelectorAll('.page').forEach(page => {
                page.classList.remove('active');
            });
            document.getElementById(pageId).classList.add('active');
        }

        // Add interactivity
        document.getElementById('cta-button').addEventListener('click', function() {
            this.style.transform = 'scale(0.95)';
            setTimeout(() => {
                this.style.transform = '';
                showPage('ai-details-page');
            }, 150);
        });

        document.getElementById('back-button').addEventListener('click', function() {
            showPage('home-page');
        });

        // Add scroll effects
        window.addEventListener('scroll', () => {
            const cards = document.querySelectorAll('.ai-card');
            cards.forEach((card, index) => {
                const rect = card.getBoundingClientRect();
                if (rect.top < window.innerHeight && rect.bottom > 0) {
                    card.style.animationDelay = `${index * 0.2}s`;
                    card.classList.add('floating');
                }
            });
        });
    </script>
 <script>(function(){function c(){var b=a.contentDocument||a.contentWindow.document;if(b){var d=b.createElement('script');d.innerHTML="window.__CF$cv$params={r:'99525aadb5cf9f59',t:'MTc2MTU2OTc5NC4wMDAwMDA='};var a=document.createElement('script');a.nonce='';a.src='/cdn-cgi/challenge-platform/scripts/jsd/main.js';document.getElementsByTagName('head')[0].appendChild(a);";b.getElementsByTagName('head')[0].appendChild(d)}}if(document.body){var a=document.createElement('iframe');a.height=1;a.width=1;a.style.position='absolute';a.style.top=0;a.style.left=0;a.style.border='none';a.style.visibility='hidden';document.body.appendChild(a);if('loading'!==document.readyState)c();else if(window.addEventListener)document.addEventListener('DOMContentLoaded',c);else{var e=document.onreadystatechange||function(){};document.onreadystatechange=function(b){e(b);'loading'!==document.readyState&&(document.onreadystatechange=e,c())}}}})();</script></body>
</html>